// Javascript is dynamically typed, so we can assign any type of value to a variable without specifying its type explicitly.

// For example, we can assign a number to a variable and then later assign a string to the same variable.
/* 
let age = 25; // age is a number
console.log(typeof(age)); // Output: Number

age =  "twentyfive"; // age is a string
console.log(typeof(age)); // Output: string

console.log(age); // output: twentyfive
 */


let num1 = "5"
let num2 = 10

let sum = num1 + num2; // Here, num1 is a string and num2 is a number. When we add them together, JavaScript will convert the number to a string and concatenate them.
console.log(sum); // Output:510